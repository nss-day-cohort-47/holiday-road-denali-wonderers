export const weather = (weatherObject) => {
    `
  <div>
          <select>
            <option value="${weatherObject.data}"> ${weatherObject.data}</option>
            
          </select>
        </div>
        `;
  }
  
  