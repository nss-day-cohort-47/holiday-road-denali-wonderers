import {getParkData} from "./data/Data Manager"

// dropdown filter
//const parkLocation = document.querySelector("#parkSelector")
//parkLocation.addEventListener("change", (event) => {
 //   if(event.target.id === parkSelector) {
  //      const parkName = (event.target.value);

  //  }
//})
//BS
//const filterParkLocations = (whatFilter) => {
//    const 
//}

export const bs = `<div>${getParkData()}</div>`
